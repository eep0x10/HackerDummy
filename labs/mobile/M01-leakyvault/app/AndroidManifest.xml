<?xml version="1.0" encoding="utf-8"?>
<!-- LeakyVault — INTENTIONALLY VULNERABLE Android app (decompiled apktool form).
     Training only. No real backend; all secret-shaped strings are placeholders. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leakyvault.app"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="22" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:debuggable="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- exported with NO permission guard: any installed app can open the
             internal admin screen directly -->
        <activity
            android:name=".AdminActivity"
            android:exported="true" />

        <!-- exported broadcast receiver, no permission -->
        <receiver
            android:name=".SyncReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.leakyvault.app.SYNC" />
            </intent-filter>
        </receiver>

        <!-- exported content provider, no read/write permission -->
        <provider
            android:name=".NotesProvider"
            android:authorities="com.leakyvault.app.notes"
            android:exported="true" />
    </application>
</manifest>
